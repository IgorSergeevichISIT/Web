import * as _zag_js_anatomy from '@zag-js/anatomy';
import { RequiredBy, PropTypes, DirectionProperty, CommonProperties, NormalizeProps } from '@zag-js/types';
import * as _zag_js_core from '@zag-js/core';
import { Machine, StateMachine } from '@zag-js/core';
import { PositioningOptions } from '@zag-js/popper';
export { Placement, PositioningOptions } from '@zag-js/popper';

declare const anatomy: _zag_js_anatomy.AnatomyInstance<"arrow" | "arrowTip" | "trigger" | "positioner" | "content">;

interface OpenChangeDetails {
    open: boolean;
}
type ElementIds = Partial<{
    trigger: string;
    content: string;
    positioner: string;
    arrow: string;
}>;
interface PublicContext extends DirectionProperty, CommonProperties {
    /**
     * The ids of the elements in the popover. Useful for composition.
     */
    ids?: ElementIds;
    /**
     * Function called when the hover card opens or closes.
     */
    onOpenChange?: (details: OpenChangeDetails) => void;
    /**
     * The duration from when the mouse enters the trigger until the hover card opens.
     * @default 700
     */
    openDelay: number;
    /**
     * The duration from when the mouse leaves the trigger or content until the hover card closes.
     * @default 300
     */
    closeDelay: number;
    /**
     * Whether the hover card is open
     */
    open?: boolean;
    /**
     * Whether the hover card is controlled by the user
     */
    "open.controlled"?: boolean;
    /**
     * The user provided options used to position the popover content
     */
    positioning: PositioningOptions;
}
interface PrivateContext {
}
type ComputedContext = Readonly<{}>;
type UserDefinedContext = RequiredBy<PublicContext, "id">;
type MachineContext = PublicContext & PrivateContext & ComputedContext;
interface MachineState {
    value: "opening" | "open" | "closing" | "closed";
    tags: "open" | "closed";
}
type State = StateMachine.State<MachineContext, MachineState>;
type Send = StateMachine.Send<StateMachine.AnyEventObject>;
type Service = Machine<MachineContext, MachineState, StateMachine.AnyEventObject>;
interface MachineApi<T extends PropTypes = PropTypes> {
    /**
     * Whether the hover card is open
     */
    open: boolean;
    /**
     * Function to open the hover card
     */
    setOpen(open: boolean): void;
    /**
     * Function to reposition the popover
     */
    reposition(options?: Partial<PositioningOptions>): void;
    getArrowProps(): T["element"];
    getArrowTipProps(): T["element"];
    getTriggerProps(): T["element"];
    getPositionerProps(): T["element"];
    getContentProps(): T["element"];
}

declare function connect<T extends PropTypes>(state: State, send: Send, normalize: NormalizeProps<T>): MachineApi<T>;

declare function machine(userContext: UserDefinedContext): _zag_js_core.Machine<MachineContext, MachineState, _zag_js_core.StateMachine.AnyEventObject>;

declare const props: ("dir" | "id" | "getRootNode" | "open" | "ids" | "onOpenChange" | "openDelay" | "closeDelay" | "open.controlled" | "positioning")[];
declare const splitProps: <Props extends Partial<UserDefinedContext>>(props: Props) => [Partial<UserDefinedContext>, Omit<Props, "dir" | "id" | "getRootNode" | "open" | "ids" | "onOpenChange" | "openDelay" | "closeDelay" | "open.controlled" | "positioning">];

export { type MachineApi as Api, type UserDefinedContext as Context, type ElementIds, type OpenChangeDetails, type Service, anatomy, connect, machine, props, splitProps };
