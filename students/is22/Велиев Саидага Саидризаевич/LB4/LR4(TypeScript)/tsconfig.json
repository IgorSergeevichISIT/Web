{
  "compilerOptions": {
    "target": "ES6",                      // Укажите версию JavaScript, в которую будет компилироваться TypeScript.
    "module": "ESNext",                   // Используем ES-модули.
    "strict": true,                       // Включить строгую проверку типов.
    "esModuleInterop": true,              // Позволяет использовать импорты CommonJS-модулей.
    "forceConsistentCasingInFileNames": true, // Проверка регистрозависимых путей.
    "skipLibCheck": true,                 // Пропуск проверки типов библиотек.
    "outDir": "./dist",                   // Директория для компилированных файлов.
    "rootDir": "./",                      // Корневая директория исходных файлов.
    "moduleResolution": "node",           // Разрешение модулей как в Node.js.
    "isolatedModules": true               // Убедиться, что каждый файл обрабатывается как отдельный модуль.
  },
  "include": [
    "./**/*.ts"                           // Включить все файлы с расширением `.ts`.
  ],
  "exclude": [
    "node_modules",                       // Исключить папку `node_modules`.
    "dist"                                // Исключить папку с собранным кодом.
  ]
}
